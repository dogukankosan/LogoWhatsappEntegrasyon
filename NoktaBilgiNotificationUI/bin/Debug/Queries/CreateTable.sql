IF NOT EXISTS (SELECT 1 FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'NKT_ORFFICHELOGICALREFWP')
BEGIN
    CREATE TABLE NKT_ORFFICHELOGICALREFWP
    (
        ID INT PRIMARY KEY IDENTITY (1,1) NOT NULL,
        FICHELOGICALREF INT NOT NULL UNIQUE,
        FICHENO VARCHAR(20),
        ACTIVESTATUS INT DEFAULT 0, 
        MESSAGEBODY VARCHAR(MAX),
		MESSAGEDATE DATETIME 
    )
END;